//
// Created by jamiec on 9/27/22.
//

#include "LowCompServices.h"

namespace services {

    std::pair<bool, std::pair<time_t, time_t>> allocate_task(model::WorkItem *pItem,
                                                             std::shared_ptr<model::ComputationDevice> sharedPtr){

        std::shared_ptr<model::Task>

        return std::make_pair(true, std::make_pair(time_t(), time_t()));
    }
} // services